@model List<Quote>
@{
}

<div class="card karus_quotes">
    <div class="card-body">
        <h5>Inspirational Quotes</h5>
        @if (Model?.Count > 0)
        {
            <ul>
                @foreach (var quote in Model)
                {
                    <li>@quote.Text</li>
                }
            </ul>
        }
    </div>
</div>